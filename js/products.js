$('.mumbai').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getsestination").val('Mumbai');
});

$('.paris').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getFlights").val('Paris');
});

$('.switzerland').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getFlights").val('Switzerland');
});

$('.beach').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getHotels").val('Beach');
});

$('.mountain').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getHotels").val('Mountain');
});

$('.starry').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getHotels").val('Starry');
});

$('.rv').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getCar").val('RV');
});

$('.jeep').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getCar").val('Jeep');
});

$('.fortuner').on('click', function(event) {
    event.preventDefault();

    $(document).scrollTop(1);
    $(".getCar").val('Fortuner');
});